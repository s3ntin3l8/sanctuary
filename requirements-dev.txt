# Dev/test dependencies for CI and local development.
#
# The standardized ci-python.yml reusable workflow installs ONLY this file when
# present (it prefers requirements-dev.txt over an editable install or
# requirements.txt). pyproject.toml here is config-only (no [project]/
# [build-system]), so `pip install -e .` would fail and nothing would install.
#
# requirements.txt pins the full runtime stack (CPU-only torch included). The
# test/lint tools below are intentionally kept OUT of requirements.txt so they
# never reach the Docker image (the Dockerfile installs requirements.txt only).
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-xdist==3.8.0
playwright==1.61.0
pytest-playwright==0.8.0
pre-commit==4.6.0
ruff==0.15.22
testcontainers[postgres]==4.14.2

# CI-only checks: the standardized ci-python.yml reusable workflow activates
# mypy/detect-secrets/pip-audit steps by presence (`command -v` guards). Without
# these, those steps silently no-op instead of failing loudly.
mypy==2.3.0
pip-audit==2.10.1
detect-secrets==1.5.0
